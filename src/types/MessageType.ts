export enum MessageType {
  WELCOME = 'welcome',
  LOCAL_NETWORKS = 'localNetworks',
  NETWORK_NAME = 'networkName',
  CLIENT_NAME = 'clientName',
  NETWORK = 'network',
  PING = 'ping',
  ENCRYPTED = 'encrypted',
}
